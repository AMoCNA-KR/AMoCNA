apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- locust-dep.yaml
- docker-stress.yaml

configMapGenerator:
- name: locust-script
  namespace: sock-shop
  files:
  - locustfile.py
