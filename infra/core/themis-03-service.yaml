apiVersion: v1
kind: Service
metadata:
  name: themis
  namespace: themis
  labels:
    app: themis
spec:
  selector:
    app: themis
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 50051
      targetPort: 50051
  type: ClusterIP
