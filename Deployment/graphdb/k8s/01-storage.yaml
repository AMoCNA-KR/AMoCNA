apiVersion: v1
kind: PersistentVolume
metadata:
  name: graphdb-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /data/graphdb          # created on the node by the deploy script
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - kube-worker-0  # change if you want a different node
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: graphdb-data
  namespace: graphdb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 5Gi
